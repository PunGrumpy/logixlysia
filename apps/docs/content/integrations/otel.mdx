---
title: OpenTelemetry
description: Correlate logs with active trace and span IDs
---

Install the optional peer dependency:

```bash
bun add @opentelemetry/api
```

Import from the `logixlysia/otel` subpath:

```ts
import logixlysia from 'logixlysia'
import { injectTraceContext } from 'logixlysia/otel'

const plugin = logixlysia()

const app = new Elysia()
  .use(plugin)
  .onRequest(({ request, store }) => {
    injectTraceContext(store.logger, request)
  })
  .get('/', () => 'ok')
```

When an active span exists, `injectTraceContext` merges `trace_id` and `span_id` into the request context bag, and they appear on the access log. Logixlysia does not start an OpenTelemetry SDK. You bring your own tracer provider and instrumentation.
