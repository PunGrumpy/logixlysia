---
interface Props {
  cls?: string
  /** Marks the span so the interactive demo script can fill it at runtime. */
  hook?: string
  text: string
}

const { cls, hook, text } = Astro.props
---

<span class={cls || undefined} data-hook={hook}>{text}</span>
