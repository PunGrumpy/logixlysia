---
interface Props {
  cls?: string;
  text: string;
  token?: string;
}

const { cls, text, token } = Astro.props;
---

<span
  data-token={token}
  class:list={[
    cls,
    token && 'rounded-sm transition-[background-color] duration-300'
  ]}
>
  {text}
</span>
