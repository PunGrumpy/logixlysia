{
  "name": "docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "blume build",
    "dev": "blume dev --port 3000",
    "clean": "git clean -xdf .blume .astro .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "blume": "latest"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.2",
    "typescript": "^6.0.3"
  }
}
