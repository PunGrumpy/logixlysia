---
"logixlysia": patch
---

Add `default` export conditions for Node and tsx resolution of `logixlysia`, `logixlysia/otel`, and `logixlysia/ai`.

Allow `Request` or WebSocket objects as keys for `mergeContext` and `getContext`.
