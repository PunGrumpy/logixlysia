---
'logixlysia': patch
---

Bump `chalk` from 5 to 6. Chalk 6 requires Node.js 22 or newer, so this raises the effective minimum runtime for `logixlysia`. The colouring API used by the logger is unchanged.
