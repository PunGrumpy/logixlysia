# Implementation Plans

Generated by the improve skill on 2026-09-19, from an audit of this repository at commit `478f40d` (v6.8.1, after the adapters / sampling / desertant releases). Numbering continues from the previous series (001–023, executed and closed 2026-08-10; its historical index lives in git history at commit `4e93d2b` and its two spike deliverables remain in `plans/spikes/`). The reference the maintainer supplied for this run was <https://github.com/evloghq/evlog>; its feature set was used as the yardstick for the direction findings below.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Selection note.** This run was non-interactive, so the plans below are the advisor's default pick — the highest-leverage clusters — rather than a maintainer selection. Findings listed under "Audited but not planned" are ready to be turned into plans on request.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| [024](024-export-http-error-and-lock-api-surface.md) | Export `HttpError` and the named `logixlysia` export; API-surface snapshot test; LICENSE in tarball; test gate on release | P1 | S | — | DONE — [PR #428](https://github.com/PunGrumpy/logixlysia/pull/428) — squash-merged 2026-09-19, reviewed 2026-09-19; factory renamed `createLogixlysiaPlugin` to satisfy Biome noRedundantDefaultExport; snapshot resolves dist/ in a child process to keep coverage clean |
| [026](026-adapter-batch-queue-ordering-and-error-routing.md) | Adapter batch queue: ordered sends, `flush()` awaits in-flight work, timer failures → `onError`, `Retry-After`, body release | P1 | M | — | DONE — [PR #426](https://github.com/PunGrumpy/logixlysia/pull/426) — squash-merged 2026-09-19, reviewed 2026-09-19 |
| [025](025-transport-flush-lifecycle.md) | Implement spike 020: `Transport.flush?/close?`, file-sink flush/close, `onStop` flush bounded by `flushTimeoutMs`, `flushLogixlysia()` | P1 | M | 026 | DONE — [PR #435](https://github.com/PunGrumpy/logixlysia/pull/435) — squash-merged 2026-09-19 (replaces #429 after #426 squash-merged; rebased over #427/#428), reviewed 2026-09-19; `flushTimeoutMs: 0` no longer reports a timeout, resolving a contradiction between plan Step 3 and Step 5 |
| [027](027-request-lifecycle-correctness.md) | Log a returned `Response`'s status; filtered custom logs no longer suppress the access log; one truthful error line after `afterHandle` errors; custom logs carry elapsed time; `store.beforeTime`; ALS reset | P1 | M | — | DONE — [PR #427](https://github.com/PunGrumpy/logixlysia/pull/427) — squash-merged 2026-09-19, reviewed 2026-09-19; `set.status` is always 200 in onAfterHandle on Elysia 1.4, so a returned Response wins unless set.status was assigned something else |
| [028](028-redaction-and-output-sanitization.md) | Redact sensitive query params; preserve `error.cause`; precise IPv4 + IPv6; sanitize console path and adapter error bodies; DSN out of errors; bound geo headers | P1 | M | — | DONE — [PR #430](https://github.com/PunGrumpy/logixlysia/pull/430) — squash-merged 2026-09-19, reviewed 2026-09-19 (1 REVISE round: first IPv6 pattern matched clock times and MAC addresses; now requires 8 groups or a `::` compression) |
| [029](029-docs-and-agent-instructions-accuracy.md) | Docs/README/SKILL.md/AGENTS.md accuracy; remove fictional `logFilter` fields; Biome as the VS Code formatter | P2 | M | 024 | DONE — [PR #434](https://github.com/PunGrumpy/logixlysia/pull/434) — squash-merged 2026-09-19 (replaces #431 after #428 squash-merged), reviewed 2026-09-19 |
| [030](030-wrap-ws-close-args-and-cleanup.md) | `wrapWs`: forward close code/reason, `try/finally` cleanup, binary payload type | P2 | S | — | DONE — [PR #439](https://github.com/PunGrumpy/logixlysia/pull/439) — squash-merged 2026-09-19, reviewed 2026-09-19 |
| [031](031-adapter-redirects-and-endpoint-validation.md) | Adapters: `redirect: 'error'` on ingest, validate endpoint URLs at construction | P2 | S | — | DONE — [PR #438](https://github.com/PunGrumpy/logixlysia/pull/438) — squash-merged 2026-09-19, reviewed 2026-09-19; `transportError` gained an `ErrorOptions` arg for `cause`; Better Stack bare-origin URLs gain a trailing slash from URL normalisation (wire-identical) |
| [032](032-rotation-timestamp-and-config-validation.md) | Non-fatal gzip in rotation; `SYS:`/`UTC:` timestamp prefixes; threshold and `maxFiles` validation | P2 | S | — | DONE — [PR #440](https://github.com/PunGrumpy/logixlysia/pull/440) — squash-merged 2026-09-19, reviewed 2026-09-19; compression-failure test stubs `fs.promises.writeFile` for `.gz` targets because the rotated filename carries an hrtime suffix |
| [033](033-color-output-tests.md) | Tests for the colorized console path | P2 | S | — | DONE — [PR #442](https://github.com/PunGrumpy/logixlysia/pull/442) — squash-merged 2026-09-19, reviewed 2026-09-19 (1 REVISE round: tests exposed that the method was padded before the colour lookup, so method colours never applied; fixed in the same PR with a changeset) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Delivery workflow (applies to every plan)

Carried over from the previous series:

- **One PR per plan.** Branch off `main` as `improve/<nnn>-<slug>`; open a separate PR targeting `main`. A dependent plan branches only after its prerequisite PR has merged. When a plan is executed via `/improve exec <nnn>`, the reviewer pushes the branch and opens the PR immediately after an APPROVE verdict (standing maintainer authorization, recorded 2026-07-27). Merging remains the maintainer's decision. PR bodies follow `.github/pull_request_template.md`, with verification-gate results under "Additional Notes".
- **Atomic conventional commits** (`fix(output): …`, `test(adapters): …`, `docs: …`). Do NOT add a `Co-Authored-By: Claude` trailer.
- **Changesets.** Any plan whose diff touches `packages/logixlysia/` adds `.changeset/<slug>.md` (the exact text is inside each plan). Bumps: **minor** for 024 (new exports) and 025 (new `Transport` members + `flushTimeoutMs`); **patch** for 026, 027, 028, and 029's README change.
- Verification gates (all green at `478f40d`): `bun run lint`, `bun run typecheck`, `cd packages/logixlysia && bun test` (395 pass), `bun run build`. Benchmarks via `bun run bench` are a manual regression check for 027 and 028.

## Dependency notes

- **026 → 025**: `onStop` calls each adapter's `flush()`; that call is only meaningful once `flush()` awaits every batch queued before it (026 Step 1). Doing 025 first would ship a shutdown hook that can resolve while sends are still in flight.
- **024 → 029**: the docs plan writes `import { HttpError } from 'logixlysia'` and keeps the README's named-import example; both are true only after 024.
- **025 and 027 both edit `src/index.ts`** (025 adds `.onStop`; 027 rewrites `onRequest`/`onAfterHandle`/`onError` and the logger wrapper). Whichever lands second rebases; the hunks do not overlap.
- **026 and 028 both edit `src/adapters/shared.ts`** (026 the queue; 028 one line in `attemptPost`). Trivial rebase.
- **029 Step 5** references `flushLogixlysia`/`onStop` only if 025 has merged; the plan says how to handle either case.

## Audited but not planned (ready on request)

Verified findings that did not make the default selection. Each is S effort unless noted; evidence is in the plan-style references below.

- **`wrapWs` drops the close code/reason and is not exception-safe** — `src/websocket/wrap-ws.ts:73–80` forwards only `ws` to `hooks.close`, while Elysia calls `close(ws, code, reason)`; user hooks run before cleanup with no `try/finally`, so a throwing hook leaks the connection's context bag and skips the "closed" line; `payloadType: typeof message` cannot distinguish binary frames. Fix: forward `(ws, code, reason)`, log `{ code, reason }`, wrap in `try/finally`, use `ArrayBuffer.isView`.
- **A failing gzip aborts retention cleanup and double-reports** — `src/output/rotation-manager.ts:80–89` reports then rethrows; `performRotation` (`:173–189`) awaits it unguarded so `maxFiles` cleanup is skipped; `file-sink.ts:163–175` reports the same error again. Fix: catch around `compressFile` in `performRotation`; report once.
- **`timestamp.translateTime` ignores pino-pretty's `SYS:`/`UTC:` prefixes** — `create-logger.ts:36–57` renders `SYS:standard` literally (no timestamp at all). Fix: strip/handle the prefix; special-case `standard`.
- **Missing config validation** — `slowThreshold > verySlowThreshold` yields a green line with a slow badge (`create-logger.ts:100–113`); numeric `maxFiles ≤ 0` deletes rotated files (`utils/rotation.ts:65–72`, `rotation-manager.ts:140–146`). Fix: validate in `resolveOptions`.
- **Credentialed POSTs follow cross-origin redirects; endpoints unvalidated** — `shared.ts:111–116` uses default `redirect: 'follow'` with vendor credential headers (`DD-API-KEY`, `X-ClickHouse-Key`, Basic auth); env-supplied URLs are never parsed at construction (`datadog.ts:61,100`, `axiom.ts:60–62`, …). Fix: `redirect: 'error'`; a `parseEndpoint()` helper that throws at construction. MED confidence on the redirect exposure (runtime-dependent).
- **Sentry: one random `trace_id` per record** — `sentry.ts:73–78` falls back to `randomTraceId()` per entry, scattering one request's logs across N synthetic traces. Fix: derive from `context.requestId` when present. MED confidence on the UX impact.
- **Colorized console path has zero test coverage** — `create-logger.ts` is at 79 % lines; every `useColors` branch is dead under `bun test` because `isTTY` is false. Fix: inject the flag and assert ANSI output. This is also the prerequisite for ever replacing `chalk` with `util.styleText`.
- **Rotation/sink error paths under-tested** — `rotation-manager.ts` 81 %, uncovered ranges are the failure branches. Fix: drive against an unwritable temp path (M).
- **Adapter timeout/abort and non-`Error` rejection untested** — `shared.ts:117–123`; `stubFetch` never rejects. Partly covered by 026 Step 4.
- **Coverage gate cannot fire** — `bunfig.toml` floor is 0.51/0.48 vs 97 % actual. Deliberately pinned to per-file minimums by plan 014; raise to ~0.90 only after the color and rotation tests above land.
- **`pino`/`pino-pretty` are loaded at import time** even though construction is lazy (`logger/index.ts:1–2`); both are hard dependencies. Investigate-only: moving them to optional peers + `createRequire` changes a public access model (`store.pino`). Not a fix plan.
- **No pre-commit hook; `.claude` hook formats the whole repo per edit** — add `lefthook` on staged files; scope the Claude hook. `turbo.json` `test` has no `inputs`/`outputs`.

## Direction (options for the maintainer, grounded in this repo vs. evlog)

Not ranked against bugs. Each would become a design/spike plan, not a build-everything plan.

1. **Route include/exclude for the access log + a tail-sampling predicate.** `LogFilter` has only `level` (`types/config.ts:32–38`); the glob compiler and matcher already exist and run per request (`sampling/glob.ts`, `sampling/index.ts`), so `logFilter.ignorePaths`/`include` is near-zero new machinery. `TailSamplingConfig` is a closed `{durationMs, paths, status}` struct; `enrichers` already accept arbitrary functions, so a `keep?: (outcome) => boolean` escape hatch is the missing symmetry. evlog: `include`/`exclude` globs and `keep` in `shared/middleware.ts`. Trade-off: a third "what gets logged" concept next to `logFilter.level` and `sampling`; exclusion vs. sampling precedence must be documented.
2. **A public transport-middleware kit.** `withRedaction` (`desertant.ts`) and `createHttpTransport`/`createBatchQueue`/`postWithRetry` (`adapters/shared.ts`, used by all nine adapters) are the same "wrap a transport" pattern, unnamed and unexported. A `logixlysia/transports` subpath exposing them lets third parties write adapter #10 without a PR here. evlog: `pipeline.ts` (`createDrainPipeline`). Trade-off: freezes `BatchTransportOptions` and flatten semantics as public contract; may be over-engineering if nine first-party adapters cover demand. Spike, not build. Sequence after 025/026.
3. **Make the logger observable to itself.** Every loss path is silent: head-sampling drops and the per-request buffer cap (`sampling/index.ts: 145–170`), desertant queue-full drops (own `onError` only), adapter batches that exhaust retries. An `onDropped({ reason, count })` sibling to `onError`, batched off the hot path, is the cheapest honest answer to "are we dropping logs?". evlog: `onDropped`, `diagnostics.ts`. Trade-off: callback first is reversible; a `diagnostics_channel` design is not.
4. **First-party test transport + contributor `create-adapter` skill.** Capture transports are hand-rolled in ~8 test files (`__tests__/sampling/plugin.test.ts:13`, `plugin/logixlysia.test.ts:7`, …) and users have no documented way to assert on logs. Nine templated adapter files with no template or checklist (bunup entry, `exports`, test, docs page, `meta.ts`, changeset) is how `HttpError` shipped unexported. evlog: `adapters/memory.ts`, `.agents/skills/create-adapter`. Trade-off: a public `logixlysia/testing` subpath is a permanent API for something mostly internal; a private `_helpers/transport.ts` may be the honest scope.

evlog features judged **not worth adopting** for an Elysia plugin: audit logging with hash chains, client/browser logging, the CLI and observability score, error catalogs (`HttpError.code`/`link` already cover the benefit), manual wide-event `emit()` (auto-emission is the documented differentiator), an `fs` adapter (file logging with rotation is core), mutation testing in CI (the API snapshot in 024 is higher leverage per hour), and a docker-compose e2e sandbox (adapters are fetch-stubbed only; revisit if a wire-format bug ships).

## Findings considered and rejected

Recorded so nobody re-audits these:

- **Adapter duplication**: all nine adapters route through `createHttpTransport` (or `otlp-core.ts` which does); each file is genuinely vendor-specific body shaping. Well-factored; nothing to consolidate.
- **Four deep-walk implementations** (`redact.ts`, `desertant.ts`, `shared.ts flattenInto`, `create-logger.ts` tree walker): different semantics on purpose (in-place with cycle guard vs. async rebuild with path-scoped `seen` vs. depth-3 flatten vs. display). A shared walker would need four behaviour flags; more complexity than the ~60 lines saved.
- **`exports` lacks a `require` condition** (attw `CJSResolvesToESM`, node10 `NoResolution`): engines `node >= 24`, peer is Elysia; no consumer is on `require()`/node10. Not worth a CJS build.
- **`chalk` → `util.styleText`**: would drop a dependency, but the color path has zero tests today; a blind rewrite of the default output. Revisit after color tests exist.
- **`bun audit` advisories** (37): all reachable only via `apps/docs` (blume/astro), `packages/bench` (evlog/express) and `apps/elysia` (`@elysiajs/swagger`); zero paths through `workspace:logixlysia`. The published package's deps (`chalk`, `pino`, `pino-pretty`) are clean.
- **`typescript` pinned `6.0.3` at root vs `^6.0.3` in leaves**: cosmetic; the consumer-facing floor is the proven peer `>=5.0`.
- **`typecheck` dependsOn `^build`**: correct — `apps/*` import the package through `exports` → `dist/`.
- **`mock.module` leakage in `__tests__/otel/otel-mock.test.ts`**: no new evidence; `otel.test.ts` already documents and tolerates it.
- **`.state('pino', …)` exposure**: documented public API; free until first access because the pino Proxy is lazy.
- **`Logger` vs `RequestScopedLogger` two shapes**: intentional. Minor gap: `TFields` typing applies only to the derived `log`, not `store.logger` — fold into whatever next touches `types/logger.ts`.
- **`beforeHandle` short-circuits losing the access log**: verified NOT a bug — Elysia 1.4 still runs `afterHandle` when `beforeHandle` returns.
- **`beforeTime ?? BigInt(0)` producing a huge duration**: `elapsedMs` treats `0n` as a sentinel and returns `0`; the only path that hits it is fixed by plan 027.
- **`desertant.ts` "poisoned promise tail"**: verified NOT a bug — the task never rejects (all failure paths are caught inside `log`), so a failing record cannot block later records.
- **`desertant.ts` shared sub-object marked `[Circular]`**: NOT a bug — the `seen` set is copied per branch, so a non-cyclic shared reference is redacted twice, not flagged.
- Carried over from the previous series (still rejected): ReDoS in redaction regexes (bounded, benchmarked linear); path traversal via rotation paths (operator config); `ai.ts` exfiltration (local only); committed secrets (re-scanned clean, no tracked `.env`); files-sdk integration; per-PR/scheduled bench in CI.

## Not audited this run

Standard depth: hotspot-weighted, key package. Not covered: `apps/docs` application code (Astro/Blume components, `blume.config.ts`), the `.github/workflows` beyond `validate.yml`/`release.yml`/`security.yml` skims (terminal-recording, labeler, docs-redeploy were not reviewed), `packages/bench` correctness, and the draft Elysia 2 migration branch (PR #371). Adapters were audited against their fetch-stubbed tests, not a live backend.
